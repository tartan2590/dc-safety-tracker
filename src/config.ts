// Set this to your Google Sheets 'Publish to the web' CSV URL.
// Example (looks like):
// https://docs.google.com/spreadsheets/d/e/XXXXXXXXXXXXXXXXXXXX/pub?gid=0&single=true&output=csv
// For first run, we default to the local sample CSV. Replace this with your Sheet later.
export const SHEET_CSV_URL = '/sample.csv'

// Optional: for GitHub Pages, set this to your repo name (used in vite.config base)
export const REPO_NAME = 'REPO_NAME'
